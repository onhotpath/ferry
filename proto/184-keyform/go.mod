module github.com/onhotpath/ferry/proto/keyform

// Throwaway prototype for issue #184. Never merges. It is deliberately outside
// the root go.work so that nothing in the shipped tree sees it; the go.work
// beside this file is what resolves core sibling-on-disk.
go 1.27
